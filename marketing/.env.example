# Copy to .env and fill in your values
# PUBLIC_ prefix = exposed to the browser (safe for client-side JS)

# Your deployed Cloudflare Worker URL
# After `wrangler deploy`, replace with the workers.dev URL or custom domain route
PUBLIC_API_URL=https://yapperhq.com
