# Cloudflare Pages custom headers
# Hashed assets from SvelteKit's static adapter are immutable — cache forever.
/_app/immutable/*
  Cache-Control: public, max-age=31536000, immutable

# SPA fallback and route HTML must always revalidate.
/index.html
  Cache-Control: no-cache

/*.html
  Cache-Control: no-cache
