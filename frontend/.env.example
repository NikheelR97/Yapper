# Yapper backend API URL
VITE_API_URL=http://localhost:8080

# WebSocket URL (same host as API, just ws:// scheme)
VITE_WS_URL=ws://localhost:8080/ws

# Firebase VAPID public key (for web push notifications)
VITE_FIREBASE_VAPID_KEY=your_vapid_public_key_here
